---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`text-xs uppercase tracking-[0.25em] font-mono font-semibold text-accent ${className}`}
>
  <slot />
</span>
